subprojects {
    buildscript {
        repositories {
            jcenter()
        }
        dependencies {
            classpath 'com.android.tools.build:gradle:2.3.1'
        }
    }

    repositories {
        jcenter()
    }

    group = GROUP
    version = VERSION_NAME
}
